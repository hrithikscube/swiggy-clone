import React from 'react'

const Title = ({ heading }) => {
    return (
        <p className='text-footerDark font-medium lg:text-lg text-base tracking-wide'>{heading}</p>
    )
}

export default Title